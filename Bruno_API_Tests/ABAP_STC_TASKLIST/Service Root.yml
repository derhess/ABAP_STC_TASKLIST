info:
  name: Service Root
  type: http
  seq: 1

http:
  method: GET
  url: "{{protocol}}://{{host}}:{{port}}/sap/opu/odata/sap/ZSTC_TM_TASKLIST_SRV/?$format=xml"
  params:
    - name: $format
      value: xml
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
